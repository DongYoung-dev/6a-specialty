<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>렝가형아's 블로그 | 상세 페이지</title>

    <!-- Bulma CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <!-- icon fontawesome CSS -->
    <script src="https://kit.fontawesome.com/60f73b0952.js" crossorigin="anonymous"></script>

    <!-- CSS 파일 경로 -->
    <link rel="stylesheet" type="text/css" href="../static/posting.css">

    <!-- ajax 사용 링크 -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>

    <!-- Google font 설정 링크 -->
    <link
        href="https://fonts.googleapis.com/css2?family=Do+Hyeon&family=Dongle:wght@300;400&family=Gamja+Flower&family=IBM+Plex+Sans+KR:wght@100&family=Jua&display=swap"
        rel="stylesheet">

    <!-- JS *정확한 용도 검색 필요-->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-cookie/1.4.1/jquery.cookie.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bs-custom-file-input/dist/bs-custom-file-input.js"></script>

    <style>
        * {
            font-family: 'Jua', sans-serif;
        }

        html,
        body {
            margin: 0;

            padding: 0;

            min-width: 100%;
            width: 100%;
            max-width: 100%;

            min-height: 100%;
            height: 100%;
            max-height: 100%;

            background-color: #F1DCBD;
        }

        .icon-text {
            position: relative;
            right: 700px;
            bottom: 230px;
        }

        .logo {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: white;
        }

        .wrap label {
            font-weight: 300;
            color: #5A3B1A;
        }

        .posting-box {
            border: 1px solid #5A3B1A;
            border-radius: 10px;
            padding: 20px;
            max-width: 500px;
            margin: 60px auto 30px auto;
        }

        a {
            text-decoration: none;
            color: black;
            font-weight: bold;
        }

        h1 {
            margin-top: 16px;
        }

        .img-container {
            width: 100%;
        }

        .img-wrap {
            max-width: 500px;
            margin: 0 auto;
        }

        #img-preview {
            margin: 34px 0 50px 0;
            width: 100%;
            height: 330px;
        }

        .url {
            margin-bottom: 16px;
        }

        /*사용자 임의 변경 불가*/
        textarea {
            resize: none;
        }

        .btn-wrap {
            margin-top: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        button {
            width: 150px;
            height: 40px;
            font-size: 15px;
            margin-left: 10px;
        }

        .footer {
            background-color: #F1DCBD;
            margin-top: 70px;
        }
    </style>

    <script>
        const pathName = window.location.pathname;
        const postId_arr = pathName.split("/")
        const postId = postId_arr[2]


        $(document).ready(function () {
            detail_page()
        })

        //받아온 name 값이랑 같은 데이터들을 현재 문서의 데이터에 넣어주고 url값을 append 해줌
        function detail_page() {
            $.ajax({
                type: "GET",
                url: `/api/post/${postId}`,  // -- 홈에서 클릭한 게시물의 이름을 받아오고 db에서 동일한 이름을 가진 데이터를 가져옴 --
                data: {},
                success: function (response) {
                    let rows = response['post']

                    document.getElementById("postId").innerHTML = rows['postId']
                    document.getElementById("title").innerHTML = rows['title']
                    document.getElementById("name").innerHTML = rows['name']
                    document.getElementById("password").innerHTML = rows['password']
                    document.getElementById("contents").innerHTML = rows['contents']
                    document.getElementById("dates").innerHTML = rows['dates']
                }
            })
        }

        //삭제하기 버튼 클릭 시 posting_delete로 name 값을 넘겨주고 alert와 함께 인덱스 페이지로 이동
        function delete_post() {
            $.ajax({
                type: 'GET',
                url: '/posting_delete',
                data: {
                    name_give: '{{ name }}'
                },
                success: function (response) {

                    alert(response['msg'])
                    window.location.href = '/'
                }
            });
        }

    </script>
</head>

<body>
    <!-- logo -->
    <div class="logo">
        <img src="../static/main_logo.png">
        <a href="/">
            <span class="icon-text">
                <span class="icon">
                    <i class="fas fa-home"></i>
                </span>
                <span>Home</span>
            </span>
        </a>
    </div>

    <!-- posting-box -->
    <section class="main-box">
        <div class="posting-box" id="posting-box">
            <div class="wrap">
                <div class="field">
                    <label class="label">게시물 번호</label>
                    <div class="control">
                        <textarea class="input" id="postId" readonly></textarea>
                    </div>
                </div>
                <div class="field">
                    <label class="label">제목</label>
                    <div class="control">
                        <textarea class="input" id="title" readonly></textarea>
                    </div>
                </div>
                <div class="field">
                    <label class="label">작성자</label>
                    <div class="control">
                        <textarea class="input" id="name" readonly></textarea>
                    </div>
                </div>
                <div class="field">
                    <label class="label">비밀번호</label>
                    <div class="control">
                        <textarea class="input" id="password" readonly></textarea>
                    </div>
                </div>
                <div class="field">
                    <label class="label">내용</label>
                    <div class="control">
                        <textarea class="input" id="contents" readonly></textarea>
                    </div>
                </div>

                <div class="field">
                    <label class="label">날짜</label>
                    <div class="control">
                        <textarea class="input" id="dates" readonly></textarea>
                    </div>
                </div>


                <div class="btn-wrap">
                    <button type="button" class="btn btn-secondary btn-lg mr-3"
                        onclick='window.location.href = "/posting_update?name="+"{{ name }}"'>
                        수정하기
                    </button>
                    <button type="button" class="btn btn-dark btn-lg" id="posting-btn" onclick="delete_post()">삭제하기
                    </button>
                </div>
            </div>


        </div>
    </section>

    <!--footer-->
    <footer class="footer" style="padding: 50px; text-align: center; text-decoration: none">
        <div class="content has-text-centered">
            <p>
                <strong>DOG DOG</strong> by <strong>Hanghae 6a_team4.</strong> The source code is licensed
                6a_team_4</a>.
                Please refer to github for details <a href="https://github.com/U-Jinyeol/6a-team4">[github link]</a>.
            </p>
        </div>
    </footer>
</body>

</html>